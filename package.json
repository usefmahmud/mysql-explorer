{
  "name": "graduation-project",
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"bun --filter server dev\" \"bun --filter client dev\""
  },
  "private": true,
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
